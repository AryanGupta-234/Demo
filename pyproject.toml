[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pre-cab-validator"
version = "0.5.2"
description = "Model-independent Pre-CAB validation engine for Normal ServiceNow Change Requests."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
llm = ["openai>=1.55", "groq>=0.30"]
docs = ["pypdf>=5.0", "openpyxl>=3.1"]
embeddings = ["sentence-transformers>=3.0"]
api = ["fastapi>=0.115", "uvicorn[standard]>=0.30"]
dev = ["pytest>=8.0", "ruff>=0.6", "mypy>=1.11", "types-openpyxl>=3.1"]
all = ["openai>=1.55", "groq>=0.30", "pypdf>=5.0", "openpyxl>=3.1", "sentence-transformers>=3.0", "fastapi>=0.115", "uvicorn[standard]>=0.30", "types-openpyxl>=3.1"]

[tool.pytest.ini_options]
pythonpath = [".", "src"]
addopts = "-q"

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E9", "F63", "F7", "F82"]

[tool.setuptools.packages.find]
where = ["src"]

[[tool.mypy.overrides]]
module = ["sentence_transformers.*", "fastapi.*", "openpyxl.*", "pypdf.*"]
ignore_missing_imports = true
